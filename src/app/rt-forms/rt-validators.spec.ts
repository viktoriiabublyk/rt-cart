import { RtValidators } from './rt-validators';
import {FormControl} from '@angular/forms';


describe('Custom validators', () => {
  let ctrl: FormControl;

  beforeEach(() => {
    ctrl = new FormControl('');
  });

});
